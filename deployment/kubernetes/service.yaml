apiVersion: v1
kind: Service
metadata:
  name: cat-dogs-service
  namespace: mlops
  labels:
    app: cat-dogs-classifier
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
spec:
  type: LoadBalancer
  selector:
    app: cat-dogs-classifier
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
  sessionAffinity: None
