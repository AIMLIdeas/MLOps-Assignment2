apiVersion: apps/v1
kind: Deployment
metadata:
  name: cat-dogs-deployment
  namespace: mlops
  labels:
    app: cat-dogs-classifier
    version: v1
  annotations:
    kubernetes.io/description: "Cat/Dogs classifier deployment on AWS EKS"
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: cat-dogs-classifier
  template:
    metadata:
      labels:
        app: cat-dogs-classifier
        version: v1
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: cat-dogs-api
        image: ghcr.io/aimlideas/mlops-assignment2/cats-dogs-classifier:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        - name: MODEL_PATH
          value: "/app/models/cat_dogs_cnn_model.pt"
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
      restartPolicy: Always
